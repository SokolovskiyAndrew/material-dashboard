import { OrderStatusIconColorDirective } from './order-status-icon-color.directive';

describe('OrderStatusIconColorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderStatusIconColorDirective();
    expect(directive).toBeTruthy();
  });
});
